meta {
  name: Remove parent department.
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/departments/:id/parent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

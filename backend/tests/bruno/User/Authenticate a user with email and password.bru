meta {
  name: Authenticate a user with email and password.
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@admin.com",
    "password": "admin"
  }
}

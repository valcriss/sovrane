meta {
  name: Add a child department.
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/departments/:id/children/:childId
  body: none
  auth: bearer
}

params:path {
  id: 
  childId: 
}

auth:bearer {
  token: {{token}}
}

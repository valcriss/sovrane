meta {
  name: Update audit config
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/audit/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "levels": ["info"],
    "categories": ["auth"],
    "updatedBy": "user1"
  }
}

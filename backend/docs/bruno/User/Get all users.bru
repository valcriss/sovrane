meta {
  name: Get all users
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/users
  body: none
  auth: bearer
}

params:query {
  ~page:
  ~limit:
  ~search:
  ~statuses:
  ~departmentIds:
  ~siteIds:
  ~roleIds:
}

auth:bearer {
  token: {{token}}
}
